apiVersion: v1
kind: Service
metadata:
  name: katm-claim
  namespace: katm
  labels:
    app: katm-claim
spec:
  type: ClusterIP
  selector:
    app: katm-claim
  ports:
    - port: 8085
      targetPort: 8085
      protocol: TCP
      name: http
