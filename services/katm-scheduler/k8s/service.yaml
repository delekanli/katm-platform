apiVersion: v1
kind: Service
metadata:
  name: katm-scheduler
  namespace: katm
  labels:
    app: katm-scheduler
spec:
  type: ClusterIP
  selector:
    app: katm-scheduler
  ports:
    - port: 8091
      targetPort: 8091
      protocol: TCP
      name: http
