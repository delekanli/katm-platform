apiVersion: v1
kind: Service
metadata:
  name: katm-auth
  namespace: katm
  labels:
    app: katm-auth
spec:
  type: ClusterIP
  selector:
    app: katm-auth
  ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP
      name: http
