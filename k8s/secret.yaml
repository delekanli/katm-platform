apiVersion: v1
kind: Secret
metadata:
  name: katm-secrets
  namespace: katm
type: Opaque
stringData:
  # Oracle Database
  DB_USER: "datas"
  DB_PASSWORD: "DATAS"
  # Redis
  REDIS_PASSWORD: "katm_redis_dev"
  # SMTP (SMTP_PASSWORD — application.yml, SMTP_PASS — docker-compose)
  SMTP_PASSWORD: "nNPCyBWO6pVCVlnyfFfs"
  SMTP_PASS: "nNPCyBWO6pVCVlnyfFfs"
  # SMS
  SMS_API_KEY: "ssp62GU"
  # MinIO / S3
  MINIO_ROOT_USER: "minioadmin"
  MINIO_ROOT_PASSWORD: "minioadmin"
  S3_ACCESS_KEY: "minioadmin"
  S3_SECRET_KEY: "minioadmin"
  # Keycloak admin
  KEYCLOAK_ADMIN: "admin"
  KEYCLOAK_ADMIN_PASSWORD: "admin"
  # Grafana
  GRAFANA_USER: "admin"
  GRAFANA_PASSWORD: "admin"
