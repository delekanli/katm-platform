apiVersion: apps/v1
kind: Deployment
metadata:
  name: katm-notification
  namespace: katm
  labels:
    app: katm-notification
spec:
  replicas: 1
  selector:
    matchLabels:
      app: katm-notification
  template:
    metadata:
      labels:
        app: katm-notification
    spec:
      containers:
        - name: katm-notification
          image: katm/katm-notification:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8088
          envFrom:
            - configMapRef:
                name: katm-config
            - secretRef:
                name: katm-secrets
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8088
            initialDelaySeconds: 40
            periodSeconds: 10
            failureThreshold: 5
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8088
            initialDelaySeconds: 60
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: katm-notification
  namespace: katm
spec:
  selector:
    app: katm-notification
  ports:
    - port: 8088
      targetPort: 8088
