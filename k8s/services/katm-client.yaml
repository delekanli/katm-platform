apiVersion: apps/v1
kind: Deployment
metadata:
  name: katm-client
  namespace: katm
  labels:
    app: katm-client
spec:
  replicas: 1
  selector:
    matchLabels:
      app: katm-client
  template:
    metadata:
      labels:
        app: katm-client
    spec:
      containers:
        - name: katm-client
          image: katm/katm-client:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8087
          envFrom:
            - configMapRef:
                name: katm-config
            - secretRef:
                name: katm-secrets
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8087
            initialDelaySeconds: 40
            periodSeconds: 10
            failureThreshold: 5
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8087
            initialDelaySeconds: 60
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: katm-client
  namespace: katm
spec:
  selector:
    app: katm-client
  ports:
    - port: 8087
      targetPort: 8087
