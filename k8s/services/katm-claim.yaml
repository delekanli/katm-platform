apiVersion: apps/v1
kind: Deployment
metadata:
  name: katm-claim
  namespace: katm
  labels:
    app: katm-claim
spec:
  replicas: 1
  selector:
    matchLabels:
      app: katm-claim
  template:
    metadata:
      labels:
        app: katm-claim
    spec:
      containers:
        - name: katm-claim
          image: katm/katm-claim:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8085
          envFrom:
            - configMapRef:
                name: katm-config
            - secretRef:
                name: katm-secrets
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8085
            initialDelaySeconds: 40
            periodSeconds: 10
            failureThreshold: 5
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8085
            initialDelaySeconds: 60
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: katm-claim
  namespace: katm
spec:
  selector:
    app: katm-claim
  ports:
    - port: 8085
      targetPort: 8085
