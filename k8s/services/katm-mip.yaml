apiVersion: apps/v1
kind: Deployment
metadata:
  name: katm-mip
  namespace: katm
  labels:
    app: katm-mip
spec:
  replicas: 1
  selector:
    matchLabels:
      app: katm-mip
  template:
    metadata:
      labels:
        app: katm-mip
    spec:
      containers:
        - name: katm-mip
          image: katm/katm-mip:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8089
          envFrom:
            - configMapRef:
                name: katm-config
            - secretRef:
                name: katm-secrets
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8089
            initialDelaySeconds: 40
            periodSeconds: 10
            failureThreshold: 5
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8089
            initialDelaySeconds: 60
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: katm-mip
  namespace: katm
spec:
  selector:
    app: katm-mip
  ports:
    - port: 8089
      targetPort: 8089
