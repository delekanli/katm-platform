apiVersion: apps/v1
kind: Deployment
metadata:
  name: katm-scheduler
  namespace: katm
  labels:
    app: katm-scheduler
spec:
  replicas: 1
  selector:
    matchLabels:
      app: katm-scheduler
  template:
    metadata:
      labels:
        app: katm-scheduler
    spec:
      containers:
        - name: katm-scheduler
          image: katm/katm-scheduler:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8091
          envFrom:
            - configMapRef:
                name: katm-config
            - secretRef:
                name: katm-secrets
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8091
            initialDelaySeconds: 40
            periodSeconds: 10
            failureThreshold: 5
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8091
            initialDelaySeconds: 60
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: katm-scheduler
  namespace: katm
spec:
  selector:
    app: katm-scheduler
  ports:
    - port: 8091
      targetPort: 8091
