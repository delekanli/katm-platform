apiVersion: apps/v1
kind: Deployment
metadata:
  name: katm-report
  namespace: katm
  labels:
    app: katm-report
spec:
  replicas: 1
  selector:
    matchLabels:
      app: katm-report
  template:
    metadata:
      labels:
        app: katm-report
    spec:
      containers:
        - name: katm-report
          image: katm/katm-report:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8084
          envFrom:
            - configMapRef:
                name: katm-config
            - secretRef:
                name: katm-secrets
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8084
            initialDelaySeconds: 40
            periodSeconds: 10
            failureThreshold: 5
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8084
            initialDelaySeconds: 60
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: katm-report
  namespace: katm
spec:
  selector:
    app: katm-report
  ports:
    - port: 8084
      targetPort: 8084
